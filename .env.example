# PostgreSQL Connection
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=norsain
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# WebSocket Server
WS_PORT=8080
WS_HOST=0.0.0.0

# Notification Channels (comma-separated)
# These should match the NOTIFY channels in your database triggers
NOTIFICATION_CHANNELS=db_changes,table_updates
