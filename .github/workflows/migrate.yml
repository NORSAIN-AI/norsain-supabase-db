name: Migrate DB
on:
  push:
    branches: [ main ]
    paths:
      - "supabase/migrations/*.sql"
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: supabase db push via OIDC + Secret Manager"
